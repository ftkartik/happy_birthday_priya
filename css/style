:root{
  --bg: linear-gradient(120deg,#fce4ec 0%, #e8f5e9 100%);
  --card-bg: rgba(255,255,255,0.9);
  --accent: #ff4081;
}
*{box-sizing:border-box}
html,body{height:100%;margin:0;font-family:Inter, system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial;scroll-behavior:smooth}
body{background:var(--bg);color:#222}

.snap-container{height:100vh;overflow-y:scroll;scroll-snap-type:y mandatory}
.snap-container > section, .snap-container > .cards-list{min-height:100vh;display:flex;align-items:center;justify-content:center;scroll-snap-align:start}

.hero, .footer{padding:24px;text-align:center}
.hero-inner{max-width:700px}
.hero{background-size:cover;background-position:center;color:var(--card-text, #fff)}
.hero .hero-inner{background:linear-gradient(rgba(0,0,0,0.25), rgba(0,0,0,0.12));padding:24px;border-radius:12px}
.hero h1{font-size:clamp(28px,6vw,48px);margin:0 0 8px}
.hero h1, .hero .sub{color:inherit}
.hero h1{font-weight:700}
.sub{color:#444;margin-top:4px}

.cards-list{flex-direction:column;padding:40px 16px;gap:40px}
.card-section{width:100%;display:flex;flex-direction:column;align-items:center;background-size:cover;background-position:center;position:relative;padding-bottom:50vh;min-height:100vh;justify-content:flex-start;perspective:1000px}
.card-section::before{content:'';position:absolute;inset:0;background:linear-gradient(rgba(0,0,0,0.18), rgba(0,0,0,0.18));pointer-events:none}
.card{width:min(720px,92%);background:rgba(255,255,255,0.12);border:1px solid rgba(255,255,255,0.35);backdrop-filter:blur(14px);border-radius:20px;padding:28px 22px;box-shadow:0 18px 40px rgba(0,0,0,0.14);transform:translateY(60px) scale(0.85) rotateX(25deg) rotateY(-8deg);opacity:0;transition:transform 1200ms cubic-bezier(0.34, 1.56, 0.64, 1),opacity 800ms ease-out,box-shadow 1200ms ease-out;color:#fff;filter:brightness(0.7)}
.card.visible{transform:translateY(0) scale(1) rotateX(0deg) rotateY(0deg);opacity:1;box-shadow:0 25px 60px rgba(0,0,0,0.3),0 0 40px rgba(255,64,129,0.2);filter:brightness(1)}
.card p{font-size:clamp(18px,3.7vw,22px);margin:0;line-height:1.5;color:#fff;animation:textReveal 1200ms cubic-bezier(0.34, 1.56, 0.64, 1) forwards}

/* last card special */
.card.special{border:2px dashed rgba(255,64,129,0.15);position:relative;overflow:visible}
#restartBtn{margin-top:12px;padding:8px 14px;border-radius:10px;border:none;background:var(--accent);color:white;font-weight:600}

.button-group{display:flex;gap:16px;margin-top:20px;flex-wrap:wrap;justify-content:center}
.btn-yes, .btn-nah{padding:12px 24px;border-radius:10px;border:none;font-weight:600;cursor:pointer;transition:all 300ms ease;font-size:14px}
.btn-yes{background:var(--accent);color:white}
.btn-yes:hover{background:#ff1470;transform:scale(1.05)}
.btn-nah{background:var(--accent);color:white}
.btn-nah:hover{background:#ff1470;transform:scale(1.05)}

/* confetti */
.confetti{position:fixed;pointer-events:none;width:10px;height:12px;opacity:1;z-index:9999;animation:confetti-fall 2500ms linear forwards}
@keyframes confetti-fall{0%{transform:translateY(0) rotate(0) ;opacity:1}100%{transform:translateY(120vh) rotate(720deg);opacity:1}}

@keyframes textReveal{0%{opacity:0;transform:scale(0.8);letter-spacing:3px}50%{letter-spacing:1px}100%{opacity:1;transform:scale(1);letter-spacing:normal}}

/* responsive tweaks */
@media (min-width:900px){.cards-list{gap:56px;padding:80px 0}}
